services:
  - type: web
    name: chat-app-backend
    env: node
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: PORT
        value: 5000
      # You will need to manually add GOOGLE_APPLICATION_CREDENTIALS containing your serviceAccountKey JSON string in the Render Dashboard
